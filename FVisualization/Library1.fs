namespace FVisualization

type CA1D() = //iteratively make images with one more line added at a time for Gif "growing"
    static member X = "F#"

type CA2D() =  //whole board per image
    static member X = "F#"

type CPPNetwork() = //I dunno know about this... find a library for this
    static member X = "F#"

type PieChart() =
    static member X = "F#"